{
    "dependencies": {
        "concurrently": "^9.1.2"
    },
    "_comment": "npm install concurrently npm run start:all — This installs the 'concurrently' package and runs both backend and frontend scripts at the same time using the 'start:all' script.",
    "scripts": {
        "start:all": "concurrently \"npm run start --prefix server\" \"npm run dev --prefix frontend\""
    }
}
